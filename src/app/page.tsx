import Hero from "@/components/sections/Hero";

export default function HomePage() {
    return (
        <>
            <Hero />
            {/* About, Work (Lumero), Vision, Contact — added in Milestones 2–3 */}
        </>
    );
}
